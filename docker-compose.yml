services:
  app:
    build: .
    container_name: langchain-claude-bridge
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8082:8082"
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434